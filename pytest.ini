[pytest]
pythonpath = .
addopts = --verbose
          --disable-warnings
          --show-capture stdout
          --cov zimran
          --cov-report html
          --cov-report term-missing
          --cov-fail-under 100
asyncio_mode = auto
